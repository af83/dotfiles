# vim: noexpandtab

[color]
	branch = auto
	diff = auto
	interactive = auto
	status = auto
[alias]
	br = branch
	ci = commit
	co = checkout
	df = diff
	fe = fetch -p -a
	lg = log -p
	sh = show
	st = status
[diff]
	renames = true
[push]
	default=tracking
